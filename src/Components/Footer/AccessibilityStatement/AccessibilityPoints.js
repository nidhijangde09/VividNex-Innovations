import React from "react";
import "./AccessibilityPoints.css";

const AccessibilityPoints = () => {
  return (
    <section className="accessibility-points">
      <ul>
        <li>
          Design site templates with an emphasis on color contrast, logical
          heading order, and configurable repeated elements.
        </li>
        <li>
          Code the templates to ensure pages are usable by a variety of input
          and output devices, such as keyboard navigation and screen readers.
        </li>
        <li>
          Ensure that all non-textual elements have a text equivalent (such as
          “alt text” for images or transcripts for videos).
        </li>
      </ul>
    </section>
  );
};

export default AccessibilityPoints;









